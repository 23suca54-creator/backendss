package com.example.jobapplication.config;

/**
 * Stub SecurityConfig: removed Spring Security references because
 * the project does not include spring-boot-starter-security in its
 * dependencies. If you add Spring Security later, replace this
 * stub with an actual SecurityFilterChain bean that calls http.cors().
 */
public class SecurityConfig {
    // No-op stub to avoid compilation errors when Spring Security is not on the classpath.
}
